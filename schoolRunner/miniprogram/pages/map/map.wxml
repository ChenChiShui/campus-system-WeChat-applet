<view class="container">
  <map
    id="map"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    scale="16"
    markers="{{markers}}"
    show-location
    bindmarkertap="onMarkerTap" 
    style="width: 100%; height: 100%;">
  </map>
</view>